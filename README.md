# Running the App

To run the application, please use the following commands:

1. Install the dependencies using the `--legacy-peer-deps` flag:
   ```bash
   npm install --legacy-peer-deps
   ```

2. Start the development server:
   ```bash
   npm run dev
   ```
